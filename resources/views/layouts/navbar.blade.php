<div class=" flex w-full h-14 absolute z-20 bg-white items-center justify-between px-20 shadow-lg ">

    {{-- <ul class="flex text-black">
        @foreach ($navbar as $name => $url)
            <li class="mr-4  hover:text-red-500 ease-in-out delay-100 duration-500"><a
                    href="{{ $url }}">{{ $name }}</a></li>
        @endforeach
    </ul> --}}

    <div class="text-black font-bold">
        <li class="mr-4 list-none font-black text-lg text-red-500"><a href="/">LEMBAH CIATER RESORT</a></li>
    </div>

    <div>
        <span class="mr-6">Hi, Yoga</span>
        <button>Log Out</button>
    </div>

</div>
